export enum Status {
  Pending = "pending",
  Rejected = "rejected",
  Approved = "approved",
  Completed = "completed",
}

export enum AnnouncementStatus {
  Pending = "pending",
  Published = "published",
}
